(in-package common-lisp-user);;;;    -------------------------------------------------------------;;;;;;;;    Table of Contents:;;;;    ;;;;    1. definition of ELSE constant;;;;    ;;;;    2. loads file containing definition of MY-LOAD (smart loader);;;;    ;;;;    3. defines portable pathnames;;;;    ;;;;    4. loads all the standard stuff;;;;    ;;;;    -------------------------------------------------------------(setq *load-verbose* t)(defconstant else t "Used for defaults in COND statements")(load "/class/sslade/vote/myload.lisp");; default pathnames for use by my-load;; Note: must add a dummy filename to avoid using directory's name ;;       as a filename in the pathname;; ---------------------------------------------------------------(defvar cl    (pathname "/class/sslade/vote/"))(defvar utils (pathname "/class/sslade/vote/utils/"))(defvar db    (pathname "/class/sslade/vote/db/"))(defvar dbs   (pathname "/class/sslade/vote/db/dbs/"))(defvar data  (pathname "/class/sslade/vote/db/data/"))(defvar pol   (pathname "/class/sslade/vote/pol/"))(defvar gen   (pathname "/class/sslade/vote/gen/"))(defvar test  (pathname "/class/sslade/vote/test/"));; allegro specific stuff(my-load utils "allegro")(my-load cl "load_utils")